{
    "title": "Upload content",
    "required": [
        "files"
    ],
    "type": "object",
    "properties": {
        "files": {
            "title": "Files",
            "type": "array",
            "items": {
                "type": "string",
                "format": "binary"
            }
        },
        "append": {
            "title": "Append",
            "type": "boolean"
        }
    }
}